# 3.1 Ingest 提示词（处理新论文）

处理论文：`raw/papers/{paper_file}.md`

请先通读再输出：
1. 这篇论文最核心 claim（一句话）
2. 相比本领域已有方法，最关键增益是什么
3. 在现有 `wiki/` 中应链接到哪些页面

然后按 `CLAUDE.md` 模板写入 `wiki/papers/`，并在末尾补充 `Gap 线索`（不空泛，需可验证）。
